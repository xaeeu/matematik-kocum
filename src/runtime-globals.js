// Global callbacks used by the module app. Keep this as a classic script so module code can resolve the names.
window.saveCoaching=window.saveCoaching||async function(e){e.preventDefault();const t=localStorage.getItem('mk_session')||'',f=new FormData(e.currentTarget),u=(()=>{try{return JSON.parse(localStorage.getItem('mk_selected_student')||'null')}catch{return null}})();const r=await fetch('/api/data',{method:'POST',headers:{'content-type':'application/json',...(t?{authorization:`Bearer ${t}`}:{})},body:JSON.stringify({type:'coachingSave',studentId:u?.id||'',weeklyGoal:f.get('weeklyGoal'),focus:f.get('focus'),habits:f.get('habits'),nextMeeting:f.get('nextMeeting')})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`İşlem başarısız (${r.status})`);location.reload()};
window.saveEvaluation=window.saveEvaluation||async function(e){e.preventDefault();const t=localStorage.getItem('mk_session')||'',f=new FormData(e.currentTarget),u=(()=>{try{return JSON.parse(localStorage.getItem('mk_selected_student')||'null')}catch{return null}})();const p={type:'evaluationSave',studentId:u?.id||'',understanding:Number(f.get('understanding')||0),homeworkRate:Number(f.get('homeworkRate')||0),attendance:Number(f.get('attendance')||0),problemSolving:Number(f.get('problemSolving')||0),focus:Number(f.get('focus')||0),consistency:Number(f.get('consistency')||0),strengths:f.get('strengths'),focusArea:f.get('focusArea'),nextGoal:f.get('nextGoal')};const r=await fetch('/api/data',{method:'POST',headers:{'content-type':'application/json',...(t?{authorization:`Bearer ${t}`}:{})},body:JSON.stringify(p)});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`İşlem başarısız (${r.status})`);location.reload()};
var saveCoaching=window.saveCoaching; var saveEvaluation=window.saveEvaluation;
